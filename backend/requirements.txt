# ─────────────────────────────────────────────
#  Core Backend
# ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
websockets==12.0
pydantic==2.7.1
pydantic-settings==2.2.1

# ─────────────────────────────────────────────
#  HTTP / Async HTTP
# ─────────────────────────────────────────────
requests==2.31.0
aiohttp==3.9.5
httpx==0.27.0

# ─────────────────────────────────────────────
#  Browser Automation (Step 3 – WhatsApp)
# ─────────────────────────────────────────────
selenium>=4.21.0             # Selenium WebDriver for Chrome control
webdriver-manager>=4.0.1     # Auto-manages ChromeDriver version matching

# ─────────────────────────────────────────────
#  File system & watching
# ─────────────────────────────────────────────
watchdog==4.0.0
pathspec==0.12.1

# ─────────────────────────────────────────────
#  System utilities
# ─────────────────────────────────────────────
psutil==5.9.8

# ─────────────────────────────────────────────
#  Screen / Automation (Optional Phase 3+)
# ─────────────────────────────────────────────
pyautogui==0.9.54
mss==9.0.1
Pillow>=10.4.0
pytesseract==0.3.10
pygetwindow>=0.0.9       # active window detection (vision module)
opencv-python-headless==4.9.0.80

# ─────────────────────────────────────────────
#  Voice — Step 6: Offline Voice I/O (Jarvis)
#  STT: faster-whisper (local Whisper model)
#  TTS: pyttsx3 (Windows SAPI5 -- no internet)
#  MIC: pyaudio wrapper around PortAudio
#
#  Install in order:
#    pip install pyttsx3
#    pip install faster-whisper
#    pip install pyaudio             (if binary wheel available for your Python)
#    -- OR on Windows if pyaudio fails:
#    pip install pipwin && pipwin install pyaudio
#
#  Whisper model downloads on first run to ~/.cache/huggingface/hub/
#  (~150MB for "base", ~75MB for "tiny")
# ─────────────────────────────────────────────
SpeechRecognition==3.10.4   # legacy Google STT (existing voice_agent.py)
pyttsx3>=2.90               # offline TTS via Windows SAPI5
faster-whisper>=1.0.1       # local Whisper STT (CTranslate2 optimised)
# pyaudio>=0.2.14           # uncomment after installing via pipwin (see above)


# ─────────────────────────────────────────────
#  Database
# ─────────────────────────────────────────────
aiosqlite==0.20.0

# ─────────────────────────────────────────────
#  AI / LLM utilities
# ─────────────────────────────────────────────
# langchain==0.2.5      # Optional – enable when needed

# ─────────────────────────────────────────────
#  Dev / Testing
# ─────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
playwright>=1.44.0
playwright>=1.44.0
PyMuPDF>=1.24.0
